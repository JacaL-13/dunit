Database seed is set to true by default. Change 'seed' variable in server/index.js to false to avoid losing data.

.env file includes:
CONNECTION_STRING=

PORT= //app port

SERVER_PORT=
//these two should be the same
REACT_APP_SERVER_PORT=